- @images.each_slice(6) do |images|
  .row#images
    - images.each do |image|
      .col-md-2.text-center#images
        = link_to image_tag(image.image_url(:small)), [@imageable, image], class: 'thumbnail', id: 'images'