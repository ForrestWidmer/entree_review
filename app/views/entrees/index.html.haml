.row
  .col-md-12.text-center
    = render 'search'
    %h1 All Entree's!
    %br
    - @entrees.each do |entree|
      .col-md-3.text-center
        .panel.panel-primary
          .panel-heading
            %h3.panel-title= entree.title
          .panel-body
            .thumbnail
              - if entree.images.empty?
                %h3 No image yet! Be the first to add one!
              - else
                = image_tag(entree.images.last.image_url)
            .caption
              %p Cras justo odio, dapibus ac facilisis in, egestas eget quam. Donec id elit non mi porta gravida at eget
              %p= link_to "Details", entree, class: 'btn btn-primary'