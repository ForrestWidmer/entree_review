.col-md-12.text-center
  = render 'search'
  %h1 All Entree's!
  %br
  - @entrees.each_slice(6) do |entree|
    .row
      - entree.each do |ent|
        .col-md-2.text-center
          .panel.panel-primary
            .panel-heading
              %h3.panel-title= ent.title
            .panel-body
              .thumbnail
                - if ent.images.empty?
                  %h3 No image yet! Be the first to add one!
                - else
                  = image_tag(ent.images.last.image_url)
              .caption
                %p Cras justo odio, dapibus ac facilisis in, egestas eget quam. Donec id elit non mi porta gravida at eget
                %p= link_to "Details", ent, class: 'btn btn-primary'